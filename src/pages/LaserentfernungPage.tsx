import TattooRemovalPage from './TattooRemovalPage';

// Laserentfernung runs with an Ärztin — client wants it as its OWN page
// (separate from landing). Route: /laserentfernung.
export default function LaserentfernungPage() {
  return <TattooRemovalPage />;
}
